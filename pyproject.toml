[project]
name = "nuh-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "linkml>=1.11.1",
]

[dependency-groups]
dev = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.0",
]
